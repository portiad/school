// Playground - noun: a place where people can play

import UIKit

for var bottlesBeer = 99; bottlesBeer >= 0; --bottlesBeer {
    if bottlesBeer == 0{
        println("No more beer left")
    }
    else {
        println("\(bottlesBeer) bottles of beer on the wall")
    }
}