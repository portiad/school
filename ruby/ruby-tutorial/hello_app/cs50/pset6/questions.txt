0. Pneumonoultramicroscopicsilicovolcanoconiosis is an English word that refers to a lung disease that is otherwise known as silicosis. It is the longest word in the English language published in a dictionary, the Oxford English Dictionary.  

1.  Getrusage() returns information describing the resources utilized by the current process, or all its terminated child processes. 

2.  16

3.  The struct require a pointer to be passed so instead of copying the value and using more memory pass the pointer value in.

4.  Reads in one letter at a time to create a word. If the word is too long it will create a new word. Ignores words with numbers. Adds a null terminator and increments the word count.

5.  fstringf will not be able to determine if there are non alpha characters in the word.

6.  So the values could not be changed.

7.  Implemented a hash table with a linked list that is indexed on the first letter of the word. Any new words get appended to the end of the linked list.

8.  It was .01 slower.

9.  Changed the indexing around.

10. I would have liked to have the load and check be alphabetical so I could know when to stop searching on check.
